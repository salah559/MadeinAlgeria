# دليل المصانع الجزائري - Made in Algeria

**منصة شاملة للبحث عن المصانع والمنشآت الصناعية في الجزائر** 🇩🇿

## ✨ المميزات

### 🌍 نظام متعدد اللغات
- **دعم 3 لغات كاملة**: العربية (RTL)، الإنجليزية، الفرنسية
- تبديل فوري بين اللغات
- حفظ تلقائي للغة المفضلة

### 🏭 دليل شامل للمصانع
- بحث متقدم بالاسم والوصف
- تصفية حسب الولاية
- تصفية حسب القطاع الصناعي
- معلومات تفصيلية لكل مصنع

### 📱 تصميم متجاوب
- يعمل على جميع الأجهزة (هاتف، تابلت، حاسوب)
- واجهة حديثة وسهلة الاستخدام
- وضع داكن/فاتح (قريباً)

### ⚙️ لوحة تحكم للإدارة
- إضافة مصانع جديدة
- تعديل بيانات المصانع
- حذف المصانع
- إدارة المنتجات

## 🛠️ التقنيات المستخدمة

### Frontend
- **React 18** + **TypeScript**
- **Vite** - أداة بناء سريعة
- **Wouter** - راوتر خفيف
- **TanStack Query** - إدارة البيانات
- **shadcn/ui** - مكونات UI
- **Tailwind CSS** - تصميم

### Backend
- **Express.js** - REST API
- **Drizzle ORM** - التعامل مع قاعدة البيانات
- **PostgreSQL** (Neon) - قاعدة البيانات

## 🚀 التطوير المحلي

### المتطلبات
- Node.js 20+
- npm أو yarn
- قاعدة بيانات PostgreSQL (Neon)

### التثبيت

```bash
# Clone المشروع
git clone <repository-url>
cd made-in-algeria

# تثبيت Dependencies
npm install

# إعداد Environment Variables
cp .env.example .env
# ثم عدل .env وأضف DATABASE_URL

# إنشاء جداول قاعدة البيانات
npm run db:push

# تشغيل التطبيق
npm run dev
```

التطبيق سيعمل على: `http://localhost:5000`

### الأوامر المتاحة

```bash
npm run dev          # تشغيل بيئة التطوير
npm run build        # بناء للإنتاج
npm run start        # تشغيل النسخة المبنية
npm run db:push      # مزامنة Schema مع قاعدة البيانات
npm run db:studio    # فتح Drizzle Studio
```

## 📦 النشر على Vercel

راجع ملف [DEPLOYMENT.md](./DEPLOYMENT.md) للحصول على دليل كامل للنشر.

### الخطوات السريعة:

1. رفع المشروع إلى GitHub
2. ربطه بـ Vercel
3. إنشاء قاعدة بيانات Neon من Vercel
4. تشغيل `npm run db:push`
5. جاهز! 🎉

## 🗂️ هيكل المشروع

```
├── client/              # Frontend (React)
│   ├── src/
│   │   ├── components/  # مكونات قابلة لإعادة الاستخدام
│   │   ├── contexts/    # React Contexts (Language)
│   │   ├── lib/         # Utilities والترجمات
│   │   ├── pages/       # صفحات التطبيق
│   │   └── App.tsx      # المكون الرئيسي
│   └── index.html
│
├── server/              # Backend (Express)
│   ├── routes.ts        # API Endpoints
│   ├── storage.ts       # طبقة التخزين (Drizzle)
│   └── index.ts         # نقطة الدخول
│
├── shared/              # كود مشترك
│   └── schema.ts        # Database Schema + Types
│
└── attached_assets/     # الصور والملفات
```

## 📡 API Endpoints

### المصانع

```
GET    /api/factories              # جلب جميع المصانع
GET    /api/factories?search=...   # البحث في المصانع
GET    /api/factories?wilaya=...   # تصفية حسب الولاية
GET    /api/factories?category=... # تصفية حسب القطاع
GET    /api/factories/:id          # جلب مصنع محدد
POST   /api/factories              # إضافة مصنع جديد
PATCH  /api/factories/:id          # تحديث مصنع
DELETE /api/factories/:id          # حذف مصنع
```

## 🌐 الصفحات المتاحة

- `/` - الصفحة الرئيسية
- `/factories` - قائمة المصانع
- `/factory/:id` - تفاصيل المصنع
- `/about` - من نحن
- `/contact` - اتصل بنا
- `/admin` - لوحة التحكم

## 🎨 التخصيص

### إضافة لغة جديدة

1. عدل `client/src/lib/translations.ts`
2. أضف الترجمات الجديدة
3. أضف خيار اللغة في `Header.tsx`

### تعديل الألوان

عدل `client/src/index.css` في قسم `:root` و `.dark`

### إضافة قطاع صناعي جديد

عدل `client/src/lib/data.ts` → `categories`

## 🤝 المساهمة

نرحب بالمساهمات! الرجاء:

1. Fork المشروع
2. إنشاء Branch جديد (`git checkout -b feature/amazing-feature`)
3. Commit التغييرات (`git commit -m 'Add amazing feature'`)
4. Push إلى Branch (`git push origin feature/amazing-feature`)
5. فتح Pull Request

## 📄 الترخيص

هذا المشروع مفتوح المصدر تحت رخصة MIT.

## 👥 المطورون

- المطور الرئيسي: [Your Name]

## 📞 التواصل

لأي استفسارات أو مشاكل، يرجى فتح Issue على GitHub.

---

**صُنع بـ ❤️ في الجزائر**
