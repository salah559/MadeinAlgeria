# دليل المصانع الجزائري - Made in Algeria

## نظرة عامة
تطبيق ويب شامل لدليل المصانع الجزائرية، يوفر منصة متقدمة للبحث عن المصانع والمنشآت الصناعية في الجزائر بمختلف القطاعات.

## المميزات الحالية

### 🔐 نظام المصادقة (Supabase)
- **تسجيل الدخول بالبريد الإلكتروني وكلمة المرور**
  - نموذج تسجيل دخول آمن ومتكامل
  - تحقق من صحة البيانات قبل الإرسال
  - رسائل خطأ واضحة للمستخدم
  
- **تسجيل الدخول عبر Google OAuth**
  - دعم تسجيل الدخول السريع بحساب Google
  - إعادة توجيه تلقائية بعد تسجيل الدخول

- **إدارة الحساب**
  - عرض معلومات المستخدم في الـ Header
  - قائمة منسدلة للمستخدم مع أفاتار
  - زر تسجيل الخروج
  - ربط صفحة Admin بالمستخدمين المسجلين

### 📋 صفحات التطبيق
- **الصفحة الرئيسية (Home)**: عرض عام للمنصة مع بحث سريع
- **المصانع (Factories)**: قائمة قابلة للبحث والتصفية
- **تفاصيل المصنع (Factory Detail)**: معلومات شاملة عن كل مصنع
- **من نحن (About)**: معلومات عن المشروع
- **اتصل بنا (Contact)**: نموذج للتواصل
- **لوحة التحكم (Admin)**: إدارة المصانع (للمستخدمين المسجلين)
- **تسجيل الدخول (Login)**: صفحة متكاملة لتسجيل الدخول
- **التسجيل (SignUp)**: صفحة إنشاء حساب جديد

## البنية التقنية

### Frontend
- **React 18** مع **TypeScript**
- **Vite** للبناء والتطوير السريع
- **Wouter** للراوتر (Routing)
- **TanStack Query** لإدارة البيانات
- **shadcn/ui** للمكونات
- **Tailwind CSS** للتنسيق
- **Supabase Client** للمصادقة

### Backend
- **Express.js** كـ REST API
- **Drizzle ORM** للتعامل مع قاعدة البيانات
- **PostgreSQL (Neon)** لتخزين البيانات
- **Supabase** لإدارة المصادقة

### أمان المفاتيح
يتم تخزين معلومات Supabase في Replit Secrets بشكل آمن:
- `VITE_SUPABASE_URL`: رابط مشروع Supabase
- `VITE_SUPABASE_ANON_KEY`: مفتاح API العام

## الملفات المهمة

### المصادقة
- `client/src/lib/supabase.ts`: إعداد Supabase client
- `client/src/contexts/AuthContext.tsx`: Context للمصادقة وإدارة حالة المستخدم
- `client/src/pages/Login.tsx`: صفحة تسجيل الدخول
- `client/src/pages/SignUp.tsx`: صفحة التسجيل

### الواجهة الأمامية
- `client/src/App.tsx`: المكون الرئيسي مع AuthProvider و Routing
- `client/src/components/Header.tsx`: شريط التنقل مع معلومات المستخدم
- `client/src/pages/`: جميع صفحات التطبيق

### Backend
- `server/index.ts`: خادم Express الرئيسي
- `server/routes.ts`: API endpoints
- `server/storage.ts`: واجهة التخزين

### قاعدة البيانات
- `shared/schema.ts`: نماذج البيانات (Drizzle Schema)
- `drizzle.config.ts`: إعدادات Drizzle ORM

## الاستخدام

### التطوير
```bash
npm run dev
```
يبدأ خادم Express و Vite على المنفذ 5000

### بناء المشروع
```bash
npm run build
```

### إدارة قاعدة البيانات
```bash
npm run db:push
```

## إعداد Google OAuth

لتفعيل تسجيل الدخول بـ Google، يجب إعداد Google OAuth في لوحة تحكم Supabase:

1. انتقل إلى Supabase Dashboard → Authentication → Providers → Google
2. فعّل "Enable Sign in with Google"
3. أدخل Client ID و Client Secret من Google Cloud Console
4. أضف Callback URL إلى Google OAuth Credentials

## التحديثات الأخيرة

### نوفمبر 7، 2025
- ✅ إضافة مصادقة Supabase كاملة
- ✅ صفحة تسجيل دخول بالبريد الإلكتروني
- ✅ صفحة تسجيل جديد
- ✅ دعم Google OAuth
- ✅ تحديث Header لعرض حالة المستخدم
- ✅ AuthContext لإدارة المصادقة عبر التطبيق
- ✅ استخدام Replit Secrets لحماية مفاتيح API

## الخطوات التالية المقترحة
- [ ] حماية صفحة Admin (تتطلب تسجيل الدخول)
- [ ] إضافة صفحة Profile للمستخدم
- [ ] تفعيل تأكيد البريد الإلكتروني
- [ ] إضافة نسيت كلمة المرور
- [ ] ربط المصانع بحسابات المستخدمين
