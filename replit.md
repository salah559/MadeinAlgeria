# دليل المصانع الجزائري - Made in Algeria

## نظرة عامة
تطبيق ويب شامل لدليل المصانع الجزائرية، يوفر منصة متقدمة للبحث عن المصانع والمنشآت الصناعية في الجزائر بمختلف القطاعات.

## المميزات الحالية

### 🔐 نظام المصادقة (Supabase)
- **تسجيل الدخول بالبريد الإلكتروني وكلمة المرور**
  - نموذج تسجيل دخول آمن ومتكامل
  - تحقق من صحة البيانات قبل الإرسال
  - رسائل خطأ واضحة للمستخدم
  
- **تسجيل الدخول عبر Google OAuth**
  - دعم تسجيل الدخول السريع بحساب Google
  - إعادة توجيه تلقائية بعد تسجيل الدخول

- **إدارة الحساب**
  - عرض معلومات المستخدم في الـ Header
  - قائمة منسدلة للمستخدم مع أفاتار
  - زر تسجيل الخروج
  - حماية صفحة Admin بقائمة بريد إلكتروني مصرح بها

### 🛡️ صلاحيات لوحة التحكم
- **الوصول المقيد للمسؤولين**: لوحة التحكم محمية ومتاحة فقط للبريدين الإلكترونيين التاليين:
  - bouazzasalah120120@gmail.com
  - madimoh44@gmail.com
- **إعادة التوجيه التلقائية**: المستخدمون غير المسجلين يتم توجيههم لصفحة تسجيل الدخول
- **صفحة رفض الوصول**: المستخدمون المسجلون بدون صلاحية يرون رسالة واضحة تفيد بعدم السماح

### 📋 صفحات التطبيق
- **الصفحة الرئيسية (Home)**: عرض عام للمنصة مع بحث سريع
- **المصانع (Factories)**: قائمة قابلة للبحث والتصفية
- **تفاصيل المصنع (Factory Detail)**: معلومات شاملة عن كل مصنع
- **من نحن (About)**: معلومات عن المشروع
- **اتصل بنا (Contact)**: نموذج للتواصل
- **لوحة التحكم (Admin)**: إدارة المصانع (مقيدة للمسؤولين المصرح بهم فقط)
- **تسجيل الدخول (Login)**: صفحة متكاملة لتسجيل الدخول
- **التسجيل (SignUp)**: صفحة إنشاء حساب جديد

## البنية التقنية

### Frontend
- **React 18** مع **TypeScript**
- **Vite** للبناء والتطوير السريع
- **Wouter** للراوتر (Routing)
- **TanStack Query** لإدارة البيانات
- **shadcn/ui** للمكونات
- **Tailwind CSS** للتنسيق
- **Supabase Client** للمصادقة

### Backend
- **Express.js** كـ REST API
- **Drizzle ORM** للتعامل مع قاعدة البيانات
- **PostgreSQL (Supabase)** لتخزين البيانات
- **Supabase** لإدارة المصادقة والقاعدة البيانات
- **Neon Serverless** للاتصال بقاعدة البيانات

### أمان المفاتيح
يتم تخزين معلومات Supabase في Replit Secrets بشكل آمن:
- `VITE_SUPABASE_URL`: رابط مشروع Supabase (للمصادقة)
- `VITE_SUPABASE_ANON_KEY`: مفتاح API العام (للمصادقة)
- `DATABASE_URL`: رابط قاعدة بيانات PostgreSQL من Supabase

## الملفات المهمة

### المصادقة والصلاحيات
- `client/src/lib/supabase.ts`: إعداد Supabase client
- `client/src/contexts/AuthContext.tsx`: Context للمصادقة وإدارة حالة المستخدم
- `client/src/pages/Login.tsx`: صفحة تسجيل الدخول
- `client/src/pages/SignUp.tsx`: صفحة التسجيل
- `client/src/pages/Admin.tsx`: لوحة التحكم مع حماية الوصول للمسؤولين (قائمة ALLOWED_ADMIN_EMAILS)

### الواجهة الأمامية
- `client/src/App.tsx`: المكون الرئيسي مع AuthProvider و Routing
- `client/src/components/Header.tsx`: شريط التنقل مع معلومات المستخدم
- `client/src/pages/`: جميع صفحات التطبيق

### Backend
- `server/index.ts`: خادم Express الرئيسي
- `server/routes.ts`: API endpoints
- `server/storage.ts`: واجهة التخزين

### قاعدة البيانات
- `shared/schema.ts`: نماذج البيانات (Drizzle Schema)
- `drizzle.config.ts`: إعدادات Drizzle ORM
- `server/storage.ts`: واجهة التخزين والاتصال بقاعدة البيانات
- `database_setup.sql`: SQL لإنشاء الجداول في Supabase

## الاستخدام

### التطوير
```bash
npm run dev
```
يبدأ خادم Express و Vite على المنفذ 5000

### بناء المشروع
```bash
npm run build
```

### إدارة قاعدة البيانات
```bash
npm run db:push
```

## إعداد Google OAuth

⚠️ **ملاحظة مهمة**: لتفعيل تسجيل الدخول بـ Google، يجب إعداد Google OAuth في لوحة تحكم Supabase.

📖 **راجع ملف `GOOGLE_OAUTH_SETUP.md` للحصول على دليل مفصل خطوة بخطوة**

### ملخص سريع:
1. إنشاء مشروع في Google Cloud Console
2. إعداد OAuth consent screen
3. إنشاء OAuth Client ID وClient Secret
4. إضافة Callback URL من Supabase إلى Google
5. إدخال Client ID و Secret في Supabase Dashboard
6. حفظ الإعدادات

**الـ Callback URL الخاص بمشروعك**: `https://hydtiskblituulfdrmzk.supabase.co/auth/v1/callback`

## التحديثات الأخيرة

### نوفمبر 7، 2025
- ✅ إضافة مصادقة Supabase كاملة
- ✅ صفحة تسجيل دخول بالبريد الإلكتروني
- ✅ صفحة تسجيل جديد
- ✅ دعم Google OAuth
- ✅ تحديث Header لعرض حالة المستخدم
- ✅ AuthContext لإدارة المصادقة عبر التطبيق
- ✅ استخدام Replit Secrets لحماية مفاتيح API
- ✅ حماية لوحة التحكم (Admin) بقائمة بريد إلكتروني مصرح بها
- ✅ إعادة توجيه تلقائية لصفحة تسجيل الدخول للمستخدمين غير المسجلين
- ✅ صفحة رفض الوصول للمستخدمين بدون صلاحية
- ✅ ربط التطبيق بقاعدة بيانات Supabase PostgreSQL
- ✅ إنشاء API endpoints كامل للمصانع (CRUD)
- ✅ دعم البحث والتصفية بالولاية والقطاع

## ⚠️ متطلبات التشغيل

لتشغيل التطبيق بشكل كامل، يجب إعداد بيانات Supabase في Replit Secrets:

### 1. إنشاء مشروع Supabase
1. اذهب إلى [supabase.com](https://supabase.com) وسجل دخول
2. أنشئ مشروعاً جديداً (New Project)
3. اختر المنطقة الأقرب لك

### 2. إعداد قاعدة البيانات
1. في لوحة تحكم Supabase، اذهب إلى **SQL Editor**
2. افتح ملف `database_setup.sql` في هذا المشروع
3. انسخ محتوى الملف والصقه في SQL Editor
4. اضغط **Run** لإنشاء الجداول والبيانات التجريبية

### 3. الحصول على المفاتيح السرية
من لوحة تحكم Supabase:
- **Settings → API**:
  - `VITE_SUPABASE_URL`: Project URL
  - `VITE_SUPABASE_ANON_KEY`: anon/public key
- **Settings → Database → Connection String**:
  - `DATABASE_URL`: Connection pooling → Transaction mode
  - انسخ الـ Connection String وتأكد من استبدال `[YOUR-PASSWORD]` بكلمة مرور قاعدة البيانات

### 4. إضافة المتغيرات في Replit Secrets
1. في Replit، اذهب إلى **Tools → Secrets**
2. أضف المتغيرات الثلاثة:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
   - `DATABASE_URL`

### 5. إعادة تشغيل التطبيق
بعد إضافة المتغيرات، سيتم إعادة تشغيل التطبيق تلقائياً

**ملاحظة**: بدون هذه المفاتيح:
- ستظهر رسالة خطأ: "Missing Supabase environment variables"
- نظام المصادقة لن يعمل
- قاعدة البيانات لن تتصل

## 🔌 API Endpoints المتاحة

### المصانع (Factories)
- `GET /api/factories` - جلب جميع المصانع (مع دعم البحث والتصفية)
  - Query parameters: `?search=...&wilaya=...&category=...`
- `GET /api/factories/:id` - جلب تفاصيل مصنع محدد
- `POST /api/factories` - إضافة مصنع جديد (للمسؤولين)
- `PATCH /api/factories/:id` - تحديث مصنع (للمسؤولين)
- `DELETE /api/factories/:id` - حذف مصنع (للمسؤولين)

## الخطوات التالية المقترحة
- ✅ حماية صفحة Admin (تتطلب تسجيل الدخول والصلاحية)
- ✅ ربط نظام الإدارة بقاعدة بيانات حقيقية (Supabase)
- [ ] ربط الصفحات الأمامية (Factories, Factory Detail) بـ API
- [ ] تحديث صفحة Admin لاستخدام API بدلاً من البيانات الوهمية
- [ ] إضافة صفحة Profile للمستخدم
- [ ] تفعيل تأكيد البريد الإلكتروني
- [ ] إضافة نسيت كلمة المرور
- [ ] ربط المصانع بحسابات المستخدمين (علاقة user-factory)
- [ ] إضافة رفع الصور للمصانع (logo, images)
